#!/usr/bin/with-contenv bashio
# shellcheck shell=bash
# ==============================================================================
# banner — Startup banner
#
# First s6-rc oneshot to run. Prints the add-on name and version so the log
# makes it obvious when a new start/restart occurred.
#
# CUSTOMIZE: update ADDON_DISPLAY_NAME to match your add-on.
# ==============================================================================

readonly ADDON_DISPLAY_NAME="APP_NAME"

# Source the full HA framework (loads all libraries including ha-log.sh)
# shellcheck source=/usr/local/lib/ha-framework/ha-framework.sh
source /usr/local/lib/ha-framework/ha-framework.sh

# Print startup banner
ha::log::banner "${ADDON_DISPLAY_NAME}"

# Log the configured log level so it is visible near the top of every log
_configured_level="$(bashio::config 'log_level' 'info')"
bashio::log.info "Log level: ${_configured_level}"
