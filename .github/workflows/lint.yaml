---
name: Lint

env:
  MONITORED_FILES: "build.yaml,config.yaml,Dockerfile,rootfs,apparmor.txt"

# yamllint disable-line rule:truthy
on:
  workflow_call:
  workflow_dispatch:

jobs:
  find:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    name: ⏳ Find addons
    outputs:
      addons: ${{ steps.addons.outputs.addons_list }}
      addons_csv: ${{ steps.addons.outputs.addons }}
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 🔍 Find changed add-ons
        id: changed_addons
        uses: ./.github/actions/find-changed-addons
        if: github.event_name != 'workflow_dispatch'
        with:
          base_ref: ${{ github.base_ref || github.ref_name }}
          monitored_files: ${{ env.MONITORED_FILES }}
          ignore: ".gemini"

      - name: 🔍 Find all add-ons
        id: find_all
        uses: ./.github/actions/find-addons
        if: github.event_name == 'workflow_dispatch'
        with:
          ignore: ".gemini"

      - name: 📋 Build addons list
        id: addons
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            addons_value="${{ steps.find_all.outputs.addons }}"
            addons_list_value="${{ steps.find_all.outputs.addons_list }}"
            echo "addons=$addons_value" >> "$GITHUB_OUTPUT"
            {
              echo "addons_list<<EOF"
              echo "$addons_list_value"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          else
            # Output both CSV and JSON formats
            addons="${{ steps.changed_addons.outputs.addons }}"
            echo "addons=$addons" >> "$GITHUB_OUTPUT"
            if [[ -n "$addons" ]]; then
              result=$(echo "$addons" | jq -R 'split(",") | map(select(length > 0))')
              {
                echo "addons_list<<EOF"
                echo "$result"
                echo "EOF"
              } >> "$GITHUB_OUTPUT"
            else
              echo "addons_list=[]" >> "$GITHUB_OUTPUT"
            fi
          fi

  permissions:
    name: 🔒 Rootfs permissions
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: find
    if: needs.find.outputs.addons_csv != ''
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6

      - name: 🔒 Check rootfs file permissions
        run: |
          failed=false
          for addon in $(echo "${{ needs.find.outputs.addons_csv }}" | tr ',' ' '); do
            rootfs="./$addon/rootfs"
            if [[ ! -d "$rootfs" ]]; then
              echo "ℹ️ No rootfs directory in $addon, skipping"
              continue
            fi
            echo "🔒 Checking executable permissions in $rootfs..."
            while IFS= read -r -d '' file; do
              if [[ ! -x "$file" ]]; then
                echo "❌ Not executable: $file"
                failed=true
              fi
            done < <(find "$rootfs" -type f -print0)
          done
          if [[ "$failed" == "true" ]]; then
            echo "❌ Some rootfs files are not executable. Run the fix-permissions workflow or chmod +x them."
            exit 1
          fi
          echo "✅ All rootfs files are executable"

  shellcheck:
    name: 🔍 Shellcheck
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: find
    if: needs.find.outputs.addons_csv != ''
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6

      - name: 🔍 Shellcheck
        run: |
          for addon in $(echo "${{ needs.find.outputs.addons_csv }}" | tr ',' ' '); do
            echo "🔍 Shellchecking $addon..."
            find "./$addon" -name "*.sh" -exec shellcheck -s bash {} +
          done

  markdown:
    name: 🔍 Markdown Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: find
    if: needs.find.outputs.addons_csv != ''
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6

      - name: 🔍 Markdown Lint
        run: |
          for addon in $(echo "${{ needs.find.outputs.addons_csv }}" | tr ',' ' '); do
            echo "🔍 Markdown linting $addon..."
            find "./$addon" -name "*.md" \
              ! -name "README.md" \
              ! -name "CHANGELOG.md" \
              ! -name "DOCS.md" \
              -exec npx markdownlint {} +
          done

  json:
    name: 🔍 JSON Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: find
    if: needs.find.outputs.addons_csv != ''
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6

      - name: 🔍 JSON Lint
        run: |
          for addon in $(echo "${{ needs.find.outputs.addons_csv }}" | tr ',' ' '); do
            echo "🔍 JSON linting $addon..."
            find "./$addon" -name "*.json" -exec cat {} \; | jq '.'
          done

  yaml:
    name: 🔍 YAML Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: find
    if: needs.find.outputs.addons_csv != ''
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6

      - name: 🔍 YAML Lint
        run: |
          for addon in $(echo "${{ needs.find.outputs.addons_csv }}" | tr ',' ' '); do
            echo "🔍 YAML linting $addon..."
            find "./$addon" \( -name "*.yaml" -o -name "*.yml" \) \
              -exec yq eval '... comments=""' --output-format=yaml {} + > /dev/null
          done

  addon-linter:
    name: 🚀 Add-on Linter
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: find
    if: needs.find.outputs.addons_csv != ''
    strategy:
      fail-fast: false
      matrix:
        addon: ${{ fromJSON(needs.find.outputs.addons) }}
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6

      - name: 🚀 Add-on Linter
        uses: frenck/action-addon-linter@v2
        with:
          path: "./${{ matrix.addon }}"

  dockerfile:
    name: 🐳 Dockerfile Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: find
    if: needs.find.outputs.addons_csv != ''
    strategy:
      fail-fast: false
      matrix:
        addon: ${{ fromJSON(needs.find.outputs.addons) }}
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6

      - name: 🐳 Dockerfile Lint
        uses: hadolint/hadolint-action@v3.3.0
        with:
          dockerfile: "./${{ matrix.addon }}/Dockerfile"
          config: './.github/.hadolint.yaml'

  build-yaml:
    name: 📋 Verify build.yaml
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: find
    if: needs.find.outputs.addons_csv != ''
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6

      - name: 📋 Verify build.yaml project key
        run: |
          for addon in $(echo "${{ needs.find.outputs.addons_csv }}" | tr ',' ' '); do
            BUILD_FILE="$addon/build.yaml"
            if [[ ! -f "$BUILD_FILE" ]]; then
              echo "❌ Error: build.yaml not found in $addon"
              exit 1
            fi

            PROJECT_VALUE=$(yq eval '.labels.project // ""' "$BUILD_FILE")

            if [[ -z "$PROJECT_VALUE" || "$PROJECT_VALUE" == "null" ]]; then
              echo "❌ Error: build.yaml in $addon is missing 'labels.project' key"
              exit 1
            fi

            echo "✅ build.yaml has project key with value: $PROJECT_VALUE"
          done
