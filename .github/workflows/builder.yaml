---
name: Builder

# yamllint disable-line rule:truthy
on:
  workflow_dispatch:
    inputs:
      addons:
        description: 'Comma-separated list of add-on names to build (leave empty for changed add-ons)'
        required: false
        type: string
  workflow_call:
    inputs:
      addons:
        description: 'Comma-separated list of add-on names to build (leave empty for changed add-ons)'
        required: false
        type: string
      base_ref:
        description: 'The base ref to compare against (for finding changed add-ons)'
        required: false
        type: string
        default: 'main'

permissions:
  contents: read
  actions: write

jobs:
  init:
    runs-on: ubuntu-latest
    name: ⏳ Initialize builds
    timeout-minutes: 10
    outputs:
      addons: ${{ steps.addon_filter.outputs.addons }}
    steps:
      - name: ⤵️ Check out the repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: 🚀 Find all add-on directories
        id: addons
        uses: ./.github/actions/find-addons
        with:
          ignore: ".gemini"

      - name: 🔍 Find changed add-ons
        id: changed_addons
        uses: ./.github/actions/find-changed-addons
        with:
          base_ref: ${{ inputs.base_ref || github.base_ref || github.ref_name }}
          ignore: ".gemini"

      - name: 🔍 Filter add-ons to build
        id: addon_filter
        run: |
          # Check if specific add-ons were requested
          if [[ -n "${{ inputs.addons }}" ]]; then
            # Get all available addons as JSON array
            all_addons="${{ steps.addons.outputs.addons_list }}"

            # Process input and filter to only valid addons (compact output)
            result=$(echo "${{ inputs.addons }}" | jq -Rc --argjson all "$all_addons" '
              split(",") |
              map(select(length > 0)) |
              map(ltrimstr(" ") | rtrimstr(" ")) |
              map(select(. as $item | $all | index($item)))
            ')

            # If no valid addons, fall back to changed addons
            addon_count=$(echo "$result" | jq 'length')
            if [[ "$addon_count" -eq 0 ]]; then
              echo "No valid add-ons specified, using changed add-ons"
              changed_addons="${{ steps.changed_addons.outputs.addons }}"
              if [[ -n "$changed_addons" ]]; then
                result=$(echo "$changed_addons" | jq -Rc 'split(",") | map(select(length > 0))')
              else
                result="[]"
              fi
            fi
          else
            # Use changed add-ons from find-changed-addons
            changed_addons="${{ steps.changed_addons.outputs.addons }}"
            if [[ -n "$changed_addons" ]]; then
              result=$(echo "$changed_addons" | jq -Rc 'split(",") | map(select(length > 0))')
            else
              result="[]"
            fi
          fi

          echo "addons=$result" >> "$GITHUB_OUTPUT"
          echo "Building add-ons: $result"

  build:
    needs: init
    runs-on: ubuntu-latest
    if: needs.init.outputs.addons != '[]'
    name: 👷‍♂️ Build ${{ matrix.addon }} for ${{ matrix.arch }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        addon: ${{ fromJson(needs.init.outputs.addons) }}
        arch: ["aarch64", "amd64"]
    steps:
      - name: ⤵️ Check out repository
        uses: actions/checkout@v6
      - name: ℹ️ Get information
        id: info
        uses: home-assistant/actions/helpers/info@master
        with:
          path: "./${{ matrix.addon }}"
      - name: 🦺 Check if add-on should be built
        id: check
        run: |
          if [[ "${{ steps.info.outputs.architectures }}" =~ ${{ matrix.arch }} ]]; then
             echo "${{ matrix.arch }} is a supported arch for ${{ matrix.addon }}, starting build"
             echo "build_arch=true" >> "$GITHUB_OUTPUT"
             echo "image=${{ steps.info.outputs.image }}" >> "$GITHUB_OUTPUT"
           else
             echo "${{ matrix.arch }} is not a valid arch for ${{ matrix.addon }}, skipping build"
             echo "build_arch=false" >> "$GITHUB_OUTPUT"
          fi
      - name: 💾 Cache Docker layers
        if: steps.check.outputs.build_arch == 'true'
        uses: actions/cache@v5
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ matrix.addon }}-${{ matrix.arch }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-${{ matrix.addon }}-${{ matrix.arch }}-
            ${{ runner.os }}-buildx-${{ matrix.addon }}-
      - name: 🔧 Build
        if: steps.check.outputs.build_arch == 'true'
        uses: home-assistant/builder@2025.11.0
        with:
          args: |
            --test \
            --${{ matrix.arch }} \
            --target /data/${{ matrix.addon }} \
            --image "${{ steps.check.outputs.image }}" \
            --docker-hub "ghcr.io/${{ github.repository_owner }}" \
            --addon
      - name: 🏥 Start container for healthcheck
        if: steps.check.outputs.build_arch == 'true'
        run: |
          # Find the newly built image by matching repo/image and arch, sorted by creation time
          BUILT_IMAGE=$(docker images \
            --filter "reference=ghcr.io/${{ github.repository_owner }}/*" \
            --format "{{.Repository}}:{{.Tag}} {{.CreatedAt}}" \
            | grep "${{ steps.check.outputs.image }}" \
            | grep -E "${{ matrix.arch }}|amd64|aarch64" \
            | sort -k2 -r | head -n1 | awk '{print $1}')

          if [[ -z "$BUILT_IMAGE" ]]; then
            echo "ERROR: Could not find built image for ${{ matrix.addon }} (${{ matrix.arch }})"
            echo "Available images:"
            docker images --format "table {{.Repository}}\t{{.Tag}}\t{{.CreatedAt}}" | head -n20
            exit 1
          fi

          echo "Found built image: $BUILT_IMAGE"
          mkdir -p /tmp/${{ matrix.addon }}-data
          docker run -d \
            --name "${{ matrix.addon }}-${{ matrix.arch }}-healthcheck" \
            -e "TZ=UTC" \
            -v "/tmp/${{ matrix.addon }}-data:/data" \
            "$BUILT_IMAGE"
      - name: 🩺 Wait for healthcheck
        id: healthcheck
        if: steps.check.outputs.build_arch == 'true'
        uses: stringbean/docker-healthcheck-action@v3.0.0
        with:
          container: ${{ matrix.addon }}-${{ matrix.arch }}-healthcheck
          wait-time: 120
          require-status: running
          require-healthy: true
        continue-on-error: true
      - name: 📊 Healthcheck status
        if: always() && steps.check.outputs.build_arch == 'true'
        run: |
          {
            echo "## Healthcheck: ${{ matrix.addon }} (${{ matrix.arch }})"
            echo "- Status: \`${{ steps.healthcheck.outputs.status }}\`"
            echo "- Healthy: \`${{ steps.healthcheck.outputs.healthy }}\`"
            if [ "${{ steps.healthcheck.outputs.healthy }}" != "true" ]; then
              echo "### Container Logs"
              echo "\`\`\`"
              docker logs "${{ matrix.addon }}-${{ matrix.arch }}-healthcheck" 2>&1 || true
              echo "\`\`\`"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
      - name: 🧹 Cleanup healthcheck container
        if: always() && steps.check.outputs.build_arch == 'true'
        run: |
          docker rm -f "${{ matrix.addon }}-${{ matrix.arch }}-healthcheck" || true
